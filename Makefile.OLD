#
# Makefile
#
CC = c99
RM = rm -f
LIBDIR = lib
SUBDIRS = lib
TESTDIR = tests
lib_LIBRARIES = libailsa.a
test_PROGRAMS = check_kv

all: $(lib_LIBRARIES)

lib libs: $(lib_LIBRARIES)

tests:	$(test_PROGRAMS)

$(lib_LIBRARIES):
	$(MAKE) -C $(LIBDIR) $@

$(test_PROGRAMS):	$(lib_LIBRARIES)
	$(MAKE) -C $(TESTDIR) $@

.PHONY: clean

clean:
	for dir in $(SUBDIRS); do \
	  $(MAKE) -C $$dir clean; \
	done
